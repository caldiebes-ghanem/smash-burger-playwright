# TC0 — Sign in  log out // in  fixture so the generated email can be re-used in login and other TCs 

# TC1 — log in in  with right credentials

# TC2 — log in in  with wrong credentials

# TC3 — Place pickup order with “Create Your Own” burger + validate details across pages

# TC4/TC5/TC6 Update quantity in cart (increase/decrease) + verify totals recalc correctly through checkout and confirmation

# TC7 — Remove an item from cart + validate empty cart and checkout is blocked/handled correctly

# TC8 — Cart Page item image validation: mock response to inject images for items that have no image


